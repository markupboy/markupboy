---
const { deeplink, title, body, url } = Astro.props
---

<dt>
  {url && (
    <a href={url} class="title">{title}</a>
    <a href={deeplink} class="permalink">&infin;</a>
  )}
  {!url && (
    <a href={deeplink} class="title">{title}</a>
  )}
</dt>
<dd class="formatted" set:html={body}></dd>
